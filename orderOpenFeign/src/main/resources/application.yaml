server:
  port: 8204

spring:
  application:
    name: feichai-order-service
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
  profiles:
    active: dev

