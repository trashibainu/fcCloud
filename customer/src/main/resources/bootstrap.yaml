server:
  port: 8300

spring:
  profiles:
    active: dev
  application:
    name: feichai-customer-service
  cloud:
    nacos:
      discovery:
        server-addr: 47.102.223.25:8848
        namespace: ${spring.profiles.active}
      config:
        server-addr: 47.102.223.25:8848
        file-extension: yaml
        namespace: ${spring.profiles.active}
#  datasource:
#    type: com.alibaba.druid.pool.DruidDataSource
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/feichai?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
#    username: root
#    password: yuanjing

management:
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    com.alibaba.nacos.client.config.impl: WARN