server:
  port: 8400

spring:
  profiles:
    active: dev
  application:
    name: feichai-message-service
#  datasource:
#    type: com.alibaba.druid.pool.DruidDataSource
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://47.102.223.25:3306/feichai?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
#    username: root
#    password: yuanjing
  cloud:
    nacos:
      discovery:
        server-addr: 47.102.223.25:8848
        namespace: ${spring.profiles.active}
      config:
        server-addr: 47.102.223.25:8848
        file-extension: yaml
        namespace: ${spring.profiles.active}
    sentinel:
      transport:
        dashboard: localhost:8080
#        dashboard: 47.102.223.25:8080
        port: 8719   #默认
management:
  endpoints:
    web:
      exposure:
        include: '*'
logging:
  level:
    com.alibaba.nacos.client.config.impl: WARN

mybatis:
  mapper-locations: classpath:mapper/*.xml
